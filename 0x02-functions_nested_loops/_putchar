#include <unistd.h>

/**
*main - prints out the character c to the output
*@c - the character to print
*
*return 1 (Success)
*on failure return -1 and set errno 
*/

int _putchar (char c)
{
return (write (1; &c; 1++));
}
