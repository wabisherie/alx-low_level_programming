#include "main.h"

/**
 * _islower : prints 1 or 0 based on input
 *
 * @c : our first parameter
 *
 * description : will find lowercase char
 *
 * return : Always (0)
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');

}
