#include "main.h"
/**
 * _isupper - checks for uppercase character
 *
 *@c : character to be tested
 *return 1 if upper of 0 if not
 */
int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))
return (1);


return (0);
}
